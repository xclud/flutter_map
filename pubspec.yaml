name: map
description: Map widget for Flutter written in 100% Dart. Supports Android, iOS, Web and Desktop.
version: 0.9.0+1
homepage: http://pwa.ir
repository: https://github.com/xclud/flutter_map

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  latlng: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
